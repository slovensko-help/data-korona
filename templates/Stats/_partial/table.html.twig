{% import "Stats/_macro/field.html.twig" as macro %}

<p class="mt-4">
    Toto je len kontrolná agregácia. V API (<a href="{{ url('app_api_vaccinations_vaccinations') }}" target="_blank">{{ path('app_api_vaccinations_vaccinations') }}</a>)
    sú dostupné všetky dáta, z ktorých si túto agregáciu môžete vytvoriť.
    Postup aktualizácie dát v API je rovnaký ako pri aktualizácii <a href="{{ path('app_front_stats_vaccinations', {'section': 'by-day-and-region'}) }}">po dňoch a krajoch</a>.
</p>

<table class="table table-striped table-hover table-sm mt-3">
    <thead>
    <tr>
        {% for headerCell in header %}
            <th>
                {{ headerCell }}
            </th>
        {% endfor %}
    </tr>
    </thead>
    {% for row in stats %}
        <tr>
            {% for key, headerCell in header %}
                <td>
                    {{ row[key] }}
                </td>
            {% endfor %}
        </tr>
    {% endfor %}
</table>